# 《工程大数据分析与应用》第02—04讲直接依赖
# 适用环境：Windows 64 位 + Python 3.12.10 + venv
# 覆盖范围：第02—04讲学生 Notebook
#
# 推荐安装命令（在已创建的 venv 中执行）：
# python -m pip install --only-binary=:all: -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#
# Python 解释器不是 pip 包，不应写成 python==3.12.10。
# 本文件锁定课程直接依赖；pip 会解析并安装其传递依赖。

# JupyterLab 与 Notebook 内核
jupyterlab==4.5.4
ipykernel==7.3.0

# 数值计算、表格处理与统计推断
numpy==2.3.5
pandas==2.2.3
scipy==1.18.0

# 第02讲环境自检使用
scikit-learn==1.9.0

# 可视化
matplotlib==3.11.1
